specVersion: "v1beta"
serviceId: "junction"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "junction-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
